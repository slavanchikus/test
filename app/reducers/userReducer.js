const initialState = {
  id: null,
  shared: false,
  email: ''
};

export default function userReducer(state = initialState, action) {
  switch (action.type) {
    default:
      break;
  }
  return state;
}
